{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "dev": "concurrently \"export PATH=\\\"/opt/homebrew/opt/postgresql@16/bin:$PATH\\\" && DATABASE_URL=\\\"postgresql://rachana@localhost:5432/healthsync\\\" PORT=3001 pnpm --filter @workspace/api-server run dev\" \"PORT=5173 BASE_PATH=\\\"/\\\" pnpm --filter @workspace/healthsync run dev\""
  },
  "private": true,
  "devDependencies": {
    "concurrently": "^9.2.1",
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  }
}

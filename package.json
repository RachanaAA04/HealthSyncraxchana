{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "workspaces": [
    "artifacts/*",
    "lib/*",
    "lib/integrations/*",
    "scripts"
  ],
  "scripts": {
    "build": "npm run typecheck && npm run build --workspaces --if-present",
    "typecheck:libs": "tsc --build",
    "typecheck": "npm run typecheck:libs && npm run typecheck --workspaces --if-present",
    "dev": "concurrently \"export PATH=\\\"/opt/homebrew/opt/postgresql@16/bin:$PATH\\\" && DATABASE_URL=\\\"postgresql://rachana@localhost:5432/healthsync\\\" PORT=3001 npm run dev -w @workspace/api-server\" \"PORT=5173 BASE_PATH=\\\"/\\\" npm run dev -w @workspace/healthsync\""
  },
  "private": true,
  "devDependencies": {
    "concurrently": "^9.2.1",
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  }
}

kind = "web"
previewPath = "/"
title = "HealthSync – AI Health Companion"
version = "1.0.0"
id = "artifacts/healthsync"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21611

[services.development]
run = "pnpm --filter @workspace/healthsync run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/healthsync", "run", "build" ]
publicDir = "artifacts/healthsync/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21611"
BASE_PATH = "/"
